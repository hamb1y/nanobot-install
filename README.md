install script for nanobot.
makes systemd unit, installs nanobot properly with seperate venv and 'nanobot' unprivileged user.

one-line install:
```
curl -fsSL https://github.com/hamb1y/nanobot-install/releases/download/0.1.2-release/install-nanobot.sh | sh
```
